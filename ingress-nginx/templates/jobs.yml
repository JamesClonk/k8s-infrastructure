#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"kind":"Job", "metadata":{"name":"ingress-nginx-admission-create"}}), expects=1
---
spec:
  ttlSecondsAfterFinished: 60

#@overlay/match by=overlay.subset({"kind":"Job", "metadata":{"name":"ingress-nginx-admission-patch"}}), expects=1
---
spec:
  ttlSecondsAfterFinished: 60
