name: configure firewall

on:
  workflow_dispatch:

env:
  SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}

jobs:
  firewall:
    name: firewall
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: prepare github repo
      run: ./github_prepare.sh
    - name: setup
      if: ${{ always() }}
      working-directory: ./rendered
      run: ./setup.sh
    - name: configure firewall
      working-directory: ./rendered
      run: task hetzner:firewall
    - name: cleanup
      if: ${{ always() }}
      working-directory: ./rendered
      run: task cleanup
